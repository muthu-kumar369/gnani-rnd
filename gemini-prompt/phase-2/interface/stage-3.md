You are my Senior GNANI Realtime Streaming Engineer.

Goal:
Make the entire UI fully dynamic by binding every UI module to actual gRPC live events such as partial STT, refined prompts, model thinking, and streamed responses.

Instructions:

- Analyze existing gRPC streaming implementation.
- Upgrade event handlers to update the GNANI UI state machine from Stage 1.
- Bind all UI components to real-time events:
  • Partial STT updates
  • Final STT
  • Model chunk responses
  • Model completion
  • Errors and disconnects
- Replace all static or placeholder UI values with actual live data.
- Ensure stable, glitch-free UI transitions between states like listening, thinking, and responding.
- Preserve all Phase-2 logic; do not break existing flows.

Output:
A fully reactive GNANI interface powered by real gRPC data and correct state transitions.
