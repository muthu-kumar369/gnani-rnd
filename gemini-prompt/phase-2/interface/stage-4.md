You are my Lead GNANI UI Architect and JARVIS Experience Designer.

Goal:
Upgrade the entire JARVIS-themed HUD interface so that every visual element dynamically reflects real GNANI states and live data instead of static or decorative animations.

Instructions:

- Analyze the current advanced HUD UI (background, avatar, waveform, mic button, console, status bar).
- Enhance each module to react dynamically to the actual state machine:
  • Idle → subtle breathing and soft pulses
  • Wake-word → intense pulse and energy sweep
  • Listening → waveform activates using real mic amplitude
  • Receiving STT → waveform shifts into "speech energy" mode
  • Thinking → avatar flicker, rotation, radar acceleration
  • Responding → high-energy avatar, vibrant waveform response mode
  • Error → red alert HUD state
- Smooth out all animations and transitions using Framer Motion.
- Maintain the neon cyan JARVIS aesthetic while upgrading clarity and responsiveness.
- Let Gemini decide optimal UI improvements based on existing code.

Output:
A cinematic, fully dynamic, JARVIS-level GNANI interface driven by real-time state.
