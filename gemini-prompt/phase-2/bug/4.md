You are an expert senior engineer specializing in:
• Real-time voice assistants (Google Assistant, Siri, Alexa level)
• Wake-word detection systems (“Hey Gnani”) in React/Electron
• gRPC bi-directional audio/text streaming
• VAD-based auto-start and auto-stop logic
• Real-time TTS streaming pipelines
• Low-latency audio playback inside Electron
• Robust buffer/queue design
• WebAudio / native audio pre-processing (AEC / NS / AGC)
• Frontend-only implementations that integrate with existing Electron + React apps

Your job is to analyze, fix, and upgrade the Gnani **frontend** voice assistant implementation while preserving **all currently working functionality**.  
We are NOT running the backend — focus only on frontend code, logic, behavior and integration points.

----------------------------------------------------------------------  
CURRENT SYSTEM FLOW (Frontend - canonical)
----------------------------------------------------------------------

1. Electron shell loads a React UI (Jarvis theme / HUD style).
2. React captures microphone input.
3. Two ways to START streaming:
   • Wake-word detection (“Hey Gnani”) — frontend-only.
   • Clicking the mic button (manual start).
4. Once triggered, audio is streamed to backend using gRPC.
   - While streaming the UI state must be: **listening**.
5. ASR/SST is handled in backend and returns final text when ready.
6. Backend processes and LLM generates response:
   - When stream stops and backend is generating response, UI state must be: **thinking**.
7. LLM returns partial text tokens (streaming).
8. Tokens are forwarded to TTS via Electron (streaming).
   - While TTS audio plays, UI state must be: **speaking**.
9. React/Electron plays audio using browser / native audio APIs.
10. After audio completes and no follow-up, UI returns to: **idle**.

----------------------------------------------------------------------  
SESSION-BASED CONTEXT AND MEMORY
----------------------------------------------------------------------

- Frontend is responsible for ephemeral session handling only:
  • Assigning a **session ID** to each conversation instance.
  • Passing session metadata (user ID, conversation ID, mic settings) with each gRPC request.
  • Maintaining temporary conversation state **for UI purposes only** (like current spoken text display, animation states, and TTS playback position).
- Backend handles persistent memory, embeddings, and LLM context.

----------------------------------------------------------------------  
REQUIREMENTS (High-level)
----------------------------------------------------------------------

- Keep the **Jarvis** visual theme and HUD (no style regressions).  
- Ensure the **state transitions** are accurate and deterministic:
  - `idle` = nothing happening
  - `listening` = mic opened and streaming audio to backend (wakeword or mic click)
  - `thinking` = streaming stopped (VAD end or manual stop) and waiting for backend response
  - `speaking` = TTS audio is playing
  - `idle` returned when audio completes and no follow-up
- Support immediate **barge-in / interruption**: user can interrupt while assistant is thinking or speaking.
- Add frontend audio pre-processing (2.1 last point): **Echo Cancellation (AEC)**, **Noise Suppression (NS)**, **Automatic Gain Control (AGC)** using WebAudio or native modules so input quality improves and ASR + wake-word stability increases.
- Implement **2.2 (Barge-in)**, **2.3 (Visual UI/Animation loop)**, **2.4 (Show TTS text being spoken)** as additive, non-destructive features.

----------------------------------------------------------------------  
CURRENT ISSUES TO FIX
----------------------------------------------------------------------
 
• No robust frontend pre-processing (AEC/NS/AGC) applied to mic stream.

----------------------------------------------------------------------  
TASKS — WHAT YOU MUST DELIVER
----------------------------------------------------------------------

1. **Echo Cancellation / Noise Suppression / AGC (2.1 last point)**  
   - Implement a frontend audio pre-processing chain (WebAudio + optional native node module) that:
     - Enables acoustic echo cancellation (where possible via constraints and WebAudio techniques).  
     - Applies noise suppression and adaptive gain control before streaming to gRPC.  
     - Works in Electron (desktop), and degrades gracefully if platform does not support hardware AEC.  
   - Provide options/toggles in the UI to enable/disable each pre-processor for testing.

2. **Barge-in / Interruption (2.2)**  
   - Implement immediate interruption behavior:
     - If user starts speaking while assistant is **speaking** (TTS) or **thinking** (LLM streaming):  
       • Instantly stop/pause TTS playback (atomic `stopTTS()`), cancel LLM stream (`cancelLLMStream()`), and transition to **listening**.  
       • Ensure current audio buffer is flushed and next microphone stream begins without race conditions.  
     - If user manually clicks mic while assistant speaking/thinking: same behavior.  
   - Implement debounced VAD to avoid spurious barge-in on short noises.  
   - Provide clear TypeScript interfaces for `stopTTS`, `cancelLLMStream`, `startStream`, `stopStream`.

3. **Visual UI / Animation Loop (2.3)**  
   - Create `GnaniAnimationController` React component (Jarvis HUD style) controlled by canonical states: `idle | listening | thinking | speaking`.  
   - State semantics (exact):
     - `idle`: nothing happening; UI idle animation only.  
     - `listening`: mic is open and streaming to backend (wakeword OR manual mic).  
     - `thinking`: audio streaming has stopped (VAD end or manual stop) and backend is processing; show thinking animation.  
     - `speaking`: TTS audio is playing; show speaking animation synced to audio levels.  
   - Animations required:
     - Idle: subtle pulse/holo glow.  
     - Listening: waveform + active mic halo.  
     - Thinking: pulsing neural dots or progress ring.  
     - Speaking: mouth/wave animation synced to audio level.  
   - Animation should be driven by small, testable hooks (e.g., `useGnaniState()`), run at 60fps, and be lightweight (no external heavy libs unless approved).  
   - Ensure animation states are strictly tied to the canonical state machine to prevent mismatches.

4. **Show TTS Text That Gnani Is Currently Speaking (2.4 )**  
   - Since SST/ASR partials are handled in backend, frontend must show the **TTS text** that Gnani is currently speaking in real time.  
   - Implement `useSpokenText()` hook which receives streaming tokens or final text from the LLM/TTS pipeline and displays the "current spoken text" progressively.  
   - Sync displayed text with audio playhead (token-to-audio alignment) when possible; if precise alignment is not available, progressively reveal tokens as they arrive from the LLM/TTS pipeline.  
   - Provide smooth fade/scroll and small highlighting of the currently spoken word when possible.

5. **TTS Streaming Stability Fixes**  
   - Add robust audio queue/buffer manager that:
     - Accepts tokenized text or audio chunks from LLM/TTS pipeline.
     - Synthesizes (or receives) audio in small frames (50–200ms) and enqueues them.
     - Ensures the audio playback buffer maintains a small lookahead (e.g., 100–300ms) to prevent underruns.
     - Supports seamless pause/stop on barge-in and resumes safely if necessary.  
   - Provide code patches for Electron audio player and React playback hook. Use WebAudio `AudioBufferSourceNode` / `AudioWorklet` for low-latency playback.

6. **State Machine & Deterministic Flow**  
   - Provide a deterministic state machine implementation to drive UI and logic transitions: `idle ↔ listening ↔ thinking ↔ speaking`.  
   - Expose events and debouncing rules (VAD thresholds, wakeword confirmation, manual click, backend response start/stop).  
   - Add unit-testable logic for transitions (pseudo tests are OK).

7. SESSION-BASED CONTEXT AND MEMORY

8. **Deliverables & Format**  
   Produce output in the following exact structure:
   • Step-by-step diagnosis of failure points (short bullets).  
   • Design notes for AEC/NS/AGC approach and graceful degradation strategy.  
   • Exact event flow diagrams (textual flow + ASCII or small diagram).  
   • Code patches (TypeScript) for:
       - WebAudio preprocessing chain (AEC/NS/AGC)  
       - VAD hook and integration with the stream lifecycle  
       - `GnaniAnimationController` component and related hooks  
       - `useSpokenText()` hook for showing spoken text  
       - TTS queue/AudioBuffer manager (Electron main or renderer as appropriate)  
       - State-machine implementation and event handlers  
   • Integration steps and counter-checks to ensure no regressions.  
   • Small test plan and debugging logs to validate each fix.  
   • Short explanation of why each change improves stability and UX.

----------------------------------------------------------------------  
CONSTRAINTS (MUST FOLLOW)
----------------------------------------------------------------------

• **Do NOT break or remove any existing working functionality.** All patches must be additive or conditional.  
• All code output must be in **TypeScript** (Electron + React).  
• Keep Jarvis UI theme intact—only enhance with animations.  
• Do not assume backend changes; work with frontend hooks/integration points only.  
• Provide clear instructions where to place each file and exactly what lines to edit.  
• If any platform-specific limitation exists (Windows/macOS/Linux), list it explicitly and provide fallbacks.  
• Ask for the minimal required artifacts before making code changes (only these):  
   1. Current wake-word detection code (frontend).  
   2. Current VAD integration code.  
   3. gRPC streaming handler interfaces (client-side).  
   4. Electron/React TTS playback implementation.  
   5. Small sample logs showing timestamps for ASR, LLM token arrival, and TTS audio chunks.

----------------------------------------------------------------------  
STARTING INSTRUCTIONS TO THE AGENT
----------------------------------------------------------------------

1. First produce the **diagnosis** and a concise **plan of changes** (file list + short rationale).  
2. Then produce **code patches** in the requested structure and place them under appropriate file paths (e.g., `/react/src/components/GnaniAnimationController.tsx`, `/react/src/hooks/useVAD.ts`, `/electron/src/audio/preprocess.ts`).  
3. Provide a small test checklist that the developer can run in the renderer with Electron dev tools (no backend running).  
4. Finally, produce a brief rollback plan if anything breaks.

Begin only after you ask for the 5 artifacts listed in CONSTRAINTS. Do not ask for anything else.
