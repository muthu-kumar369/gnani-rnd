You are an expert senior engineer specializing in:
• Real-time voice assistants (Google Assistant, Siri, Alexa level)
• Wake-word detection systems (“Hey Gnani”) in React/Electron
• gRPC bi-directional audio/text streaming
• VAD-based auto-start and auto-stop logic
• Real-time TTS streaming pipelines
• Low-latency audio playback inside Electron
• Robust buffer/queue design

Your job is to analyze, fix, and upgrade my entire frontend voice assistant
implementation while preserving all currently working functionalities.

We are NOT running the backend right now.  
Focus ONLY on frontend code, logic, behavior, and architecture.

----------------------------------------------------------------------
CURRENT SYSTEM FLOW (Frontend)
----------------------------------------------------------------------

1. Electron shell loads a React UI.
2. React captures microphone input.
3. Two ways to START streaming:
   • Wake-word detection (“Hey Gnani”) – frontend-only.
   • Clicking the mic button manually.
4. Once triggered, audio is streamed to backend using gRPC.
5. ASR sends back streaming text.
6. Text is streamed to the LLM using gRPC.
7. LLM returns partial text tokens.
8. Tokens are sent to TTS through Electron.
9. TTS streams audio chunks back.
10. React/Electron plays the audio using browser audio APIs.

----------------------------------------------------------------------
CURRENT STOPPING LOGIC (Needs improvement)
----------------------------------------------------------------------

Two ways to STOP streaming:
1. **Automatic stop via VAD**
   - When VAD detects real “end of speech”
   - Then the backend should process the query and respond.
2. **Manual stop by clicking mic off**
   - Should immediately end audio stream
   - Backend then finalizes LLM → TTS processing

You must improve these flows and make them behave like
Google Assistant / Siri real-time conversational mode.

----------------------------------------------------------------------
CURRENT ISSUES TO FIX
----------------------------------------------------------------------

• Wake-word detection may be unreliable on frontend.
• gRPC streaming pipeline works, but TTS playback:
   - sometimes speaks one word then stops,
   - sometimes cuts mid-sentence,
   - sometimes never begins playback.
• Buffer underruns or race conditions likely.
• LLM → TTS → Audio playback pipeline needs to be as smooth as
  top assistants (no gaps, no jitter, no abrupt cut-offs).

----------------------------------------------------------------------
YOUR TASKS
----------------------------------------------------------------------

1. Verify, fix, or reimplement the wake-word system (“Hey Gnani”)
   in frontend code:
   - Ensure accurate detection.
   - No false triggers.
   - No regression of working behavior.

2. Review and improve both START mechanisms:
   - Wake-word start,
   - Manual mic start.

3. Review and improve both STOP mechanisms:
   - VAD-based automatic stop (end-of-speech detection),
   - Manual mic-off stop.

4. Fix/improve streaming TTS pipeline inside Electron:
   - Smooth continuous audio playback.
   - No early stopping.
   - No first-word-only issue.
   - Proper chunk buffering and queueing.
   - Real-time rolling buffer like Google/Siri.

5. Implement Google/Siri-style:
   - streaming token → streaming TTS → streaming audio playback
   - minimal latency (150–300ms)
   - rolling audio buffers
   - no blocking or waiting for full text

6. Provide the solution in this exact structured format:
   • Step-by-step diagnosis
   • Verification of wake-word detection
   • Verification of start/stop logic (manual + VAD + wake-word)
   • Diagnosis of TTS cut-off issues
   • Updated architecture diagram
   • Updated event flow for:
       - Wake-word start
       - Manual start
       - VAD stop
       - Manual stop
   • Code patches for:
       - Wake-word detection system
       - VAD integration
       - gRPC handlers
       - Electron TTS pipeline
       - Audio queue and buffer
       - React audio playback
   • A final improved architecture identical to top assistants
   • Clear explanation of why each change improves behavior

7. Strict constraints:
   • DO NOT break or remove any existing working functionality.
   • Only fix bugs and enhance reliability.
   • All output code must be in TypeScript (Electron + React).
