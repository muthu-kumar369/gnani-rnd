You are my Senior GNANI Full-Stack Engineer.

Goal:
Analyze my existing Electron + React frontend project and prepare it for:

1. Authentication implementation using REST APIs.
2. gRPC bidirectional audio streaming integration.
3. Keeping the existing WebSocket code, but disabling any active usage.
4. Ensuring NO existing feature or flow breaks.

Instructions:

- Analyze the complete current project directory.
- DO NOT change or delete any existing folders unless absolutely needed.
- Do not modify “scripts”, “gemini-prompts”, “.venc”, or other utility/system folders.
- Identify existing structure and prepare the project for:
  • API service layer
  • gRPC client layer
  • Auth context / token management
  • Navigation flow
- Create any missing files needed for future stages, but keep them empty or with skeleton code.
- Add comments where future work will be implemented.
- Keep all WebSocket files but ensure no component calls or initializes them.

Output:
Make all necessary updates to prepare the frontend codebase for the next stages without breaking any existing logic.
