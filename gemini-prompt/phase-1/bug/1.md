Fix the following Electron + Vite + React microphone and preload issues in my app.

Here are the exact errors I need fixed:

1. Electron Security Warning:
   “Insecure Content-Security-Policy” — either CSP missing or unsafe-eval enabled.
   Fix by giving me the correct secure CSP for Vite + Electron.

2. AudioWorklet error:
   “Unable to load a worklet's module. AudioWorklet failed, falling back to ScriptProcessorNode.”
   I need the correct structure for where to place the worklet JS file,
   and the correct absolute path to load it in Vite + Electron.

3. IPC errors:
   “[PRELOAD] Unknown IPC send channel: mic:start” and “mic:stop”.
   Fix the main.js IPC registration, preload.js exposure, and React usage.

4. Buffer error in renderer:
   “Uncaught ReferenceError: Buffer is not defined”.
   Convert the microphone Float32Array to a format that works in the browser
   OR give me a safe way to polyfill Buffer.

5. ScriptProcessorNode:
   Provide a modern fallback pattern:
   - try AudioWorkletNode
   - fallback to ScriptProcessorNode only if worklet fails

Give me:

- Updated main.js
- Updated preload.js
- Correct IPC handlers for mic:start and mic:stop
- Correct React code for microphone handling
- Correct CSP to remove security warnings
- Correct worklet load path
- Explanation of each fix (short)

Make the final output production-ready for Electron.
